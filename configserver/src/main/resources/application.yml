server:
  port: 8888
spring:
  application:
    name: configserver
  cloud:
    bus:
      trace:
        enable: true
    config:
      server:
        git:
          uri: https://github.com/yongyong849876282/orderingsystem.git #git仓库地址
          searchPaths: config #仓库路径
          username: 849876282@qq.com #git仓库用户名
          password: wy849876282 #git仓库密码
      label: master #仓库的分支
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/